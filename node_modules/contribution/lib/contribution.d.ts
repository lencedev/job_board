/// <reference types="node" />
import { IncomingMessage } from 'http';
import { GitHubStats } from './transformers';
interface Options {
    onSuccess?: (stats: GitHubStats) => unknown;
    onFailure?: (error: IncomingMessage) => unknown;
}
declare const fetchStats: (username: string, options?: Options) => Promise<GitHubStats>;
export { GitHubStats, fetchStats };
